<div>
  <a href="/memos/new" 
  style=
  "display: inline-block;
  width: auto;
  padding: 5px;
  background-color: buttonface;
  border: 1px solid #000;
  text-decoration: none;
  font-size: 15px;
  margin: 0px 10px;" >新規作成</a>
</div>

<ol>
  <% @memos.each do |id, memo| %>
    <li>
      <a href="/memos/<%= id %>"><%= CGI.escapeHTML(memo["title"]) %></a>
    </li>
  <% end %>
</ol>
